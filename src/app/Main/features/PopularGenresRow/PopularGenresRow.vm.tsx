import type { FC } from "react";

interface Props {}

export const PopularGenresRowVM: FC<Props> = ({}) => {
  return (
    <section aria-labelledby="popular-heading">
      <h2 id="popular-heading">Популярное сейчас</h2>
    </section>
  );
};
